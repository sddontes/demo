<!-- 分享弹框 -->
  <!-- <view class="commodity_screen" bindtap="hideModal" wx:if="{{showModalStatus}}"></view>
  <view animation="{{animationData}}" class="commodity_attr_box" wx:if="{{showModalStatus}}">
    <view class="pop_item flex">
      <view bindtap="_success" class="cell_cn">
        <image src="/images/share01.png" class="pop_icon"></image>
        <view>朋友圈</view>
      </view>
      <button open-type="share" hover-class='none'  class="cell_cn" wx:if="{{showfriend}}">
        <image src="/images/share02.png" class="pop_icon"></image>
        <view>微信好友</view>
      </button>
      <view bindtap='_delete' wx:if="{{showdelete}}"   class="cell_cn">
        <image src="/images/share03.png" class="pop_icon"></image>
        <view>删除</view>
      </view>
      <view bindtap='_report' wx:if="{{showreport}}"   class="cell_cn">
        <image src="/images/share04.png" class="pop_icon"></image>
        <view>举报</view>
      </view>
    </view>
    <view class="pop_cancel" bindtap="hideModal">取消</view>
  </view> -->

  <view class="commodity_screen" bindtap="hideModal" wx:if="{{showModalStatus}}"></view>
  <view animation="{{animationData}}" class="commodity_attr_box" wx:if="{{showModalStatus}}">
    <view class="pop_cancel" bindtap="_edit" wx:if="{{showedit}}">编辑</view>
    <view class="pop_cancel" bindtap="_delete" wx:if="{{showdelete}}">删除</view>
    <view class="pop_cancel" bindtap="_report" wx:if="{{showreport}}">举报</view>
    <view class="pop_cancel" bindtap="hideModal">取消</view>
  </view>




